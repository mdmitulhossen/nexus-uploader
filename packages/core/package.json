{
  "name": "@nexus-uploader/core",
  "version": "2.0.0",
  "description": "Core client library for Nexus Uploader - handles API communication and chunking logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepare": "npm run build"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "jest": "^30.1.3",
    "lerna": "^9.0.0",
    "ts-jest": "^29.4.2",
    "typescript": "^5.4.5"
  },
  "files": [
    "dist"
  ]
}
